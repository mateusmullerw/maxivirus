import styled from "styled-components";

export const Filters = styled.div`
    width: 100%;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 16px;
    margin: 24px 0;

`;